# Agent Profile — Dr. Watson (Claude-M3)
#
# Authority capabilities. Strictly declarative — hand-authored by Judge.
# What Watson is *allowed to do*, not what the node *can do*.

agent: watson
provides:
  - lotw_write_access
  - qrz_read_access
  - eqsl_read_access
  - adif_generation
  - training_execution
  - commit_to_feature_branch
max_trust_tier: core
