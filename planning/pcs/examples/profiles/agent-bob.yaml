# Agent Profile — Bob (Claude-9975)
#
# Authority capabilities. Strictly declarative — hand-authored by Judge.

agent: bob
provides:
  - cuda_execution
  - rpm_packaging
  - tag_and_release
  - commit_to_feature_branch
  - pypi_read_access
max_trust_tier: core
